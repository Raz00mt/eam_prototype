services:
  postgres:
    image: postgres:16
    container_name: diploma_postgres
    environment:
      POSTGRES_DB: diploma
      POSTGRES_USER: diploma
      POSTGRES_PASSWORD: diploma
    ports:
      - "5433:5432"
    volumes:
      - diploma_pgdata:/var/lib/postgresql/data

volumes:
  diploma_pgdata: